# 百輪展示最佳化巡檢表

這份表不是要求每次真的手動跑 100 次，而是把三隊 App 帶去比賽前最容易出問題的地方拆成 100 個檢查點。每次大改後至少跑「必跑」區塊；比賽前一天跑完整表。

## 必跑自動檢查

1. `node scripts/competition-readiness-check.mjs` 通過。
2. `CHECK_PUBLIC_URLS=1 node scripts/competition-readiness-check.mjs` 在部署後通過。
3. `node scripts/build-github-pages.mjs` 通過。
4. `node scripts/pages-artifact-check.mjs` 通過。
5. `node scripts/mobile-layout-check.mjs` 通過。
6. `node scripts/github-prepublish-check.mjs` 通過。
7. GitHub Actions `Demo Check` 成功。
8. GitHub Actions `Deploy Student App URLs` 成功。
9. 三個 App 公開網址回傳 200。
10. 三個學生講稿頁公開網址回傳 200。

## 共用操作體驗

11. 手機寬度不水平爆版。
12. 平板寬度導覽不擋內容。
13. 桌面寬度主要資訊不過度分散。
14. 主要按鈕高度適合手指點擊。
15. 重置按鈕不會誤觸。
16. 匯出成功有明確回饋。
17. 匯入成功有明確回饋。
18. localStorage 空資料可恢復。
19. localStorage 壞資料可恢復。
20. 無網路時仍能講完展示流程。

## 學生講稿

21. 每組學生知道自己的公開操作網址。
22. 每組學生知道自己的講稿網址。
23. 每組能在 3 分鐘內講完。
24. 每組知道第一句作品定位。
25. 每組知道誰負責操作。
26. 每組知道誰負責回答硬體問題。
27. 每組能說明目前完成狀態。
28. 每組能說明 Arduino 下一階段。
29. 每組不宣稱尚未完成的實機能力。
30. 每組能在操作亂掉時重置資料。

## App 1：AI 自動板擦機器人

31. 首頁能說清楚白板 AI 助教定位。
32. 範例白板內容能產生分析。
33. 攝影機不可用時可改用手動/範例內容。
34. Gemini key 缺少時 fallback 不阻斷。
35. 白板分析能保存為課堂紀錄。
36. 教師看板能看到保留/可擦建議。
37. 教師決策能先保存。
38. 機器人任務能被送出或留下 fallback log。
39. 搜尋能找到剛剛的紀錄。
40. AI 小老師能依課堂內容回覆。
41. 學習單能產生複習題。
42. 匯出 JSON 能下載。
43. 備份能寫入本機 bridge。
44. 匯入能恢復資料。
45. `/api/ready` 在 production bridge 可用。
46. `/api/robot/commands` 與 firmware 指令表一致。
47. 無 Arduino 時不顯示不可理解錯誤。
48. 有 Arduino 時可以測 `SHOW_ON`。
49. 有 Arduino 時可以測 `ERASE_ALL`。
50. 有 Arduino 時可以測 `PAUSE_TASK`。

## App 2：校園服務機器人

51. 首頁能說清楚校園中控台定位。
52. 配送下單能建立訂單。
53. 配送下單會扣庫存。
54. 配送下單會建立任務。
55. 配送下單會更新機器人狀態。
56. 配送下單會新增硬體指令 log。
57. 庫存不足不會誤派機器人。
58. 追蹤頁能完成送達。
59. 完成送達後機器人回待命。
60. 教學頁點名可操作。
61. 學生提問回覆可操作。
62. 分心提醒處理可操作。
63. 清潔排程可修改。
64. 廣播任務可建立。
65. 智慧鐘聲可切換。
66. 派遣巡邏可建立任務。
67. 安全封鎖會更新狀態。
68. 報表頁能看到操作成果。
69. 匯出/匯入展示 state 可用。
70. 重置展示資料可回到初始狀態。

## App 3：AI 校園心靈守護者

71. 總覽能說清楚匿名與非診斷。
72. 穩定度摘要可讀。
73. 預警中心能選提醒。
74. 處置 checklist 能勾選。
75. 佈署關懷會新增支持方案。
76. 佈署關懷會留下硬體提示紀錄。
77. 自我照護能做心情簽到。
78. 匿名心情牆能新增投稿。
79. 匿名心情牆支持按鈕可操作。
80. 聊天支持能回覆考試壓力。
81. 聊天支持不宣稱診斷。
82. 節點頁能顯示各節點。
83. 離線節點能重新連線。
84. 隱私模式可切換。
85. 匯出 JSON 可下載。
86. 匯入 JSON 可恢復。
87. 重置可回到匿名示範資料。
88. Firebase 不可用時不阻斷。
89. Gemini 不可用時不阻斷。
90. 無 Arduino 時留下 fallback 硬體紀錄。

## 評審問答與硬體銜接

91. 能說明三個 App 的主題彼此不同。
92. 能說明三個 App 都是 local-first 展示版。
93. 能說明不是正式雲端多人系統。
94. 能說明 App 1 的 bridge 是共用硬體 gateway。
95. 能說明 App 2 會送配送、廣播、巡邏、安全指令。
96. 能說明 App 3 只做柔和提示，不顯示敏感個資。
97. 能說明 Arduino 未連線時為何仍可展示。
98. 能說明接上 UNO R4 後下一步要校準哪些實體動作。
99. 能說明 demo 資料可以重置，不會污染下一輪展示。
100. 能在最後 10 秒說出「軟體流程已完整打通，硬體連動是下一階段實機校準」。
